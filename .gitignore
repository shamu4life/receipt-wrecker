node_modules/
package-lock.json
.wrangler/
.dev.vars*
.env*
.DS_Store
.claude/

# Local browser-automation scratch (Playwright MCP)
.playwright-mcp/

# Print-engine measurement scratch — render test pages and their PDF/PNG output
# here (see CLAUDE.md, "Measuring against the real engine"). Keeping it in one
# ignored directory stops stray t*.html / *.pdf / *-1.png artifacts from being
# swept into a commit, which has happened.
.render/

# python bytecode from importing tools/rig.py as a module
__pycache__/
