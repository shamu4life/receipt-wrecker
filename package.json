{
  "name": "receipt-wrecker",
  "version": "0.8.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "render": "python3 tools/rig.py",
    "payload": "node tools/payload.mjs",
    "calibrate": "python3 tools/calibrate.py",
    "test:browser": "node --test test-browser/composer.spec.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "wrangler": "^4"
  }
}
